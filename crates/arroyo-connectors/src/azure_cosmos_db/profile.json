{
    "type": "object",
    "title": "AzureCosmosDbConfig",
    "properties": {
        "endpointUrl": {
            "type": "string",
            "title": "Endpoint URL",
            "description": "The Azure Cosmos DB endpoint URL",
            "examples": ["https://myaccount.documents.azure.com:443/"],
            "format": "uri"
        },
        "primaryKey": {
            "type": "string",
            "title": "Primary Key",
            "description": "The primary key for authentication with Azure Cosmos DB",
            "format": "var-str"
        },
        "database": {
            "type": "string",
            "title": "Database",
            "description": "The Azure Cosmos DB database name",
            "examples": ["MyDatabase"]
        },
        "container": {
            "type": "string",
            "title": "Container",
            "description": "The Azure Cosmos DB container name",
            "examples": ["MyContainer"]
        },
        "throughput": {
            "type": "integer",
            "title": "Throughput (RU/s)",
            "description": "Optional throughput configuration in Request Units per second",
            "minimum": 400,
            "examples": [400, 1000, 4000]
        }
    },
    "required": ["endpointUrl", "primaryKey", "database", "container"],
    "sensitive": ["primaryKey"]
}